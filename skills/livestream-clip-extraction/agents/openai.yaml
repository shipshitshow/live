interface:
  display_name: "Livestream Clip Extraction"
  short_description: "Find discovery-first clips from long streams"
  default_prompt: "Use $livestream-clip-extraction to turn this livestream transcript or topic file into ranked Shorts and cutdown packages."

policy:
  allow_implicit_invocation: true
